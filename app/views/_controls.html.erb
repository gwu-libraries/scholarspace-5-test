<%# Intentionally empty. Home/About/Help/Contact links and the search bar
    previously rendered here now live in the masthead header (branding + search).
    The static page routes/controllers/views remain in the codebase for later use. %>
